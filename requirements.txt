# base packages
wheel
opencv-python~=4.9.0
requests
tqdm
vector~=1.0.0
scipy
filterpy

# extra realsense
pyrealsense2-macosx; platform_system == "Darwin"
pyrealsense2; platform_system != "Darwin"

# extra azure
pyk4a-bundle~=1.3.0.2; platform_system == "Linux"
pyk4a-bundle~=1.5; platform_system == "Windows"

# extra depthai
depthai~=2.25

# extra openvino
openvino~=2024.0.0

# extra mediapipe
mediapipe~=0.10.9.0

# extra onnx
onnxruntime~=1.17.0

# extra onnx-gpu
onnxruntime~=1.17.1; platform_system == "Darwin"
onnxruntime-gpu~=1.17.1; platform_system != "Darwin"

# extra onnx-directml
onnxruntime~=1.17.1; platform_system == "Darwin"
onnxruntime-directml~=1.17.1; platform_system != "Darwin"

# extra media
moviepy
vidgear[core]

# extra numba
numba

# extra fbs
pyopengl; platform_system == "Darwin" or platform_system == "Windows"
syphon-python~=0.1.1; platform_system == "Darwin"
SpoutGL~=0.0.6; platform_system == "Windows"

# extra faiss
faiss-cpu
