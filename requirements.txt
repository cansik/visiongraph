# base packages
wheel
opencv-python
numpy
requests
tqdm
vector>=0.8.5
scipy
filterpy

# extra realsense
pyrealsense2; platform_system != "Darwin"
pyrealsense2-macosx; platform_system == "Darwin"

# extra azure
pyk4a-bundle>=1.3.0.2; platform_system == "Windows" or platform_system == "Linux"

# extra openvino
openvino-arm>=2022.1.0; platform_system == "Darwin" and platform.machine == 'arm64'
openvino>=2022.1.0; platform_system != "Darwin" and platform.machine != 'arm64'

# extra mediapipe
mediapipe-silicon>=0.8.9; platform_system == "Darwin" and platform.machine == 'arm64'
mediapipe>=0.8.9; platform_system != "Darwin" and platform.machine != 'arm64'

# extra onnx
onnxruntime==1.10.0

# extra media
moviepy

# extra numba
numba