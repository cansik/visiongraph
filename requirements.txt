# base packages
wheel
opencv-python~=4.7.0; python_version <= "3.9"
opencv-python~=4.10.0; python_version > "3.9"
requests
tqdm
vector~=1.5.0
scipy
filterpy

# extra realsense
pyrealsense2-macosx; platform_system == "Darwin"
pyrealsense2; platform_system != "Darwin"

# extra azure
pyk4a-bundle~=1.3.0.2; platform_system == "Linux" and python_version <= "3.10"
pyk4a-bundle~=1.5; platform_system == "Windows"

# extra depthai
depthai~=2.28; platform_system != "Darwin" or platform.machine == "arm64"

# extra openvino
openvino~=2024.3.0

# extra mediapipe
mediapipe~=0.10.14

# extra onnx
onnxruntime~=1.19.0

# extra onnx-gpu
onnxruntime~=1.19.0; platform_system == "Darwin"
onnxruntime-gpu~=1.19.0; platform_system != "Darwin"

# extra onnx-directml
onnxruntime~=1.19.0; platform_system == "Darwin" or platform_system == "Linux"
onnxruntime-directml~=1.19.0; platform_system == "Windows"

# extra media
moviepy
vidgear[core]

# extra numba
numba

# extra fbs
pyopengl; platform_system == "Darwin" or platform_system == "Windows"
syphon-python~=0.1.1; platform_system == "Darwin"
SpoutGL~=0.1.0; platform_system == "Windows"

# extra faiss
faiss-cpu
