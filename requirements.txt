# base packages
opencv-python~=4.7.0; python_version <= "3.9"
opencv-python~=4.10.0; python_version > "3.9"
requests
tqdm
vector~=1.5.2
scipy
numpy

# extra realsense
pyrealsense2-macosx; platform_system == "Darwin"
pyrealsense2; platform_system != "Darwin"

# extra azure
pyk4a-bundle~=1.5; platform_system == "Windows" or platform_system == "Linux"

# extra depthai
depthai~=2.29; platform_system != "Darwin" or platform.machine == "arm64"

# extra openvino
openvino~=2024.5.0

# extra mediapipe
mediapipe~=0.10.18

# extra onnx
onnxruntime~=1.20.1

# extra onnx-gpu
onnxruntime~=1.20.1; platform_system == "Darwin"
onnxruntime-gpu~=1.20.1; platform_system != "Darwin"

# extra onnx-directml
onnxruntime~=1.20.1; platform_system == "Darwin" or platform_system == "Linux"
onnxruntime-directml~=1.20.1; platform_system == "Windows"

# extra media
moviepy
vidgear[core]

# extra numba
numba

# extra fbs
pyopengl; platform_system == "Darwin" or platform_system == "Windows"
syphon-python~=0.1.1; platform_system == "Darwin"
SpoutGL~=0.1.0; platform_system == "Windows"

# extra faiss
faiss-cpu

# extra mot
filterpy